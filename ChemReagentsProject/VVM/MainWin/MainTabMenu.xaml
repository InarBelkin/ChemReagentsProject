<UserControl x:Class="ChemReagentsProject.VVM.MainWin.MainTabMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:viewmodels="clr-namespace:ChemReagentsProject.VVM.MainWin"
             xmlns:local="clr-namespace:ChemReagentsProject.VVM.MainWin"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.DataContext>
        <viewmodels:MainTabMenuVM></viewmodels:MainTabMenuVM>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button Content="Реактивы" Background="{DynamicResource SecondaryHueMidBrush}" Foreground="{DynamicResource SecondaryHueMidForegroundBrush}" Cursor="Hand" />
            <Button Content="Рецепты"/>
            <Button Content="Поставщики"/>
            <Button Content="Растворы"/>
            <Button Content="Отчёты"/>
        </StackPanel>
        <ContentControl Content="{Binding CurrentPage}"/>
        <!--<TabControl TabStripPlacement="Left" SelectionChanged>
            
        </TabControl>-->
    </Grid>
</UserControl>
